name: simulator-verification

on:
  pull_request:
  push:
    branches:
      - main
      - "cursor/**"

jobs:
  simulator-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install -r requirements.txt

      - name: Run unit tests
        run: |
          python3 -m unittest discover -s tests -p 'test_*.py'

      - name: Import compile benchmark (120-row synthetic sheet)
        run: |
          python3 tools/import/benchmark_compile.py --rows 120

      - name: Commissioning report export smoke (empty stub)
        run: |
          tools/simulator/commissioning_export_smoke.sh

      - name: Docker BACnet device smoke (probe-bip)
        run: |
          tools/simulator/docker_bacnet_smoke.sh

      - name: Run simulator matrix
        run: |
          python3 tools/simulator/run_matrix.py --output-dir artifacts/simulator

      - name: Upload simulator artifacts
        uses: actions/upload-artifact@v4
        with:
          name: simulator-matrix-artifacts
          path: artifacts/simulator/
          if-no-files-found: error
